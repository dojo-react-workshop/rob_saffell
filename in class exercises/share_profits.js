function getMaxprofit (arr) {
	let buy =arr[0];
	let sell = 0;
	let max = null;

	for(let i =0; i<arr.length-1; i++) {

		for (let j= i+1; j<arr.length; j++ ) {
			buy=arr[i];
			sell=arr[j];

			if()
		}
	}

}