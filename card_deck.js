'use strict'

function Card_Deck (deck, id, suit, card) {

	const deck_index = [];

		const card_deck = {
			id: id,
			suit: suit,
			card: card	
		}
		for (i=1; i<53; i++) {
			if (i<14) {
				
		}
	}


}
var decky = new Card_Deck(0,0,0,0);
console.log(decky.shuffle());